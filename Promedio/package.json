{
  "name": "promedio",
  "version": "1.0.0",
  "description": "Main webdev application for calculation of academic performance",
  "main": "server.js",
  "scripts": {
	  "setup": "yarn && yarn --cwd ./frontend",
    "start": "node backend/server.js",
    "server": "nodemon backend/server.js",
    "client": "yarn --cwd ./frontend start",
    "dev": "concurrently \"yarn server\" \"yarn client\""
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "body-parser": "^1.20.0",
    "dotenv": "^16.0.2",
    "express": "^4.18.1",
    "express-async-handler": "^1.2.0",
    "mongoose": "^6.6.1"
  },
  "devDependencies": {
    "concurrently": "^7.4.0",
    "nodemon": "^2.0.20"
  }
}
